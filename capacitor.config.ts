import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sicreyd.app',
  appName: 'SICREYD',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
